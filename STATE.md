# Current State

Round: 10 — **实验 2**（Round 1 / 2 属实验 1，已结束）

Status: **waiting_for_user** — 正式猜测已提交，等待裁判判定

> **协作方**：GPT 侧为 **GPT-5.6 Sol**，已建立直接通道（`GPT_SOL_ONLINE.md`），
> 可直接读本机游戏数据、可用 git 直写仓库。

## 实验编号

| 实验 | 回合 | 单位 | 状态 |
|---|---|---|---|
| 实验 1 | `001` – `002` | Mech Engineer | ✅ 已结束（Round 2 命中） |
| **实验 2** | **`003` 起** | **未公开** | 🟡 等待判定 |

## Confirmed clues（实验 2）—— 原样记录裁判原话

| 回合 | 问题 | User 原话 |
|---|---|---|
| Round 3 | 怎么移动 / 本质是什么 | **「（奔跑）」** |
| Round 3 | 主要做什么 / 最突出特点 | **「（牺牲自己，并完成目标）」** |
| Round 4 | 完成目标时具体发生什么 | **「（它变成了别的东西）」** |
| Round 4 | 什么条件下牺牲自己 | **「（到指定地点自动触发）」** |
| Round 5 | 转化物是什么性质 | **「（你说的前三个方向都可以，第四个不确定）」** |
| Round 5 | 奔跑阶段它本身是什么 | **「（它一开始是地面上的移动单位跑过去）」** |
| Round 6 | 触发后留下什么 | **「（没有什么效果，就是寻路到指定地点开始转化）」** |
| Round 6 | 题目范围 | **「（原版沙盒编辑器里可以造）」** |
| Round 7 | 工厂来源 | **「不是以上答案，可以从沙盒编辑器直接放出来，也可以工厂里出来，但不是你说的工厂」** |
| Round 7 | 自动还是手动 | **「玩家手动点『部署』，它到位后才开始转化」** |
| Round 8 | 纯原版还是含 mod | **「纯原版——游戏自带单位，不含 mod」** |
| Round 8 | 转化是否可逆 | **「不可逆——变过去就回不来了（不可撤销）」** |
| Round 9 | 判定上一猜 | **「不是实验悬浮型气垫船」** + 三条新约束 |
| Round 9 | 三条新约束 | ①「能从工厂里造出来，只是我不想直接告诉你生产它的工厂叫啥」②「它的设计观感就是在地面上跑」③「我曾后悔给它的一个指令，想要撤销发现不能」 |

## 已否决的猜测

| 回合 | 猜测 | 判定 |
|---|---|---|
| Round 9 | `experimentalGunship`（实验悬浮型气垫船） | ❌ **错误** |
| Round 10 初版 | `modularSpider`（模块化蜘蛛） | ⬇️ 降为次选（`[action_reclaim]` 可见且退款 50%，不足以解释「想撤销发现不能」） |

## 🎯 当前正式猜测

### `bugSpore`（虫族孢子 / 消耗型建造单位）

**机制链**：地面奔跑 → 玩家下达建造命令 → 寻路到指定地点 → 施工 → **原单位被消耗** → 留下持久建筑。

**关键字段（本机数据核实）**：

| 字段 | 值 |
|---|---|
| `movementType` | **`LAND`**（地面） |
| `dieOnConstruct` | **`true`**（建造时死亡 ← 字面即「牺牲自己」） |
| `useAsBuilder` | **`true`** |
| `canBuild_1..4` | `bugExtractor` / **`bugNest`（生产）** / `bugGenerator`（功能） / `bugTurret`（防御） |
| `builtFrom_1_name` | `bugNest`（虫巢，虫族生产建筑） |
| 所属 | `assets\units\classic_bugs\` —— **本体数据，非 mod** |

**排他性**：全原版**只有两个**单位带 `dieOnConstruct`（`bugSpore`、`bugFly`）。
`bugFly` 是 `AIR`，而裁判明确「在地面上跑」⇒ **`bugSpore` 是唯一解**。

## ✅ Sol（GPT-5.6）的独立复核结论

Sol 提交 `turns/009_gpt.md`，确认：

1. 该单位在本体 `assets\units\classic_bugs\` 下，**不是已启用的外部 mod**；
2. 配置含 `movementType: LAND`、`dieOnConstruct: true`、`useAsBuilder: true`；
3. 机制链「地面奔跑 → 下达建造命令 → 到点施工 → 原单位被消耗 → 留下持久建筑」
   **在原版 1.15 数据层面成立**；
4. 该内部名出现在 `assets\tilesets\units.tsx`，并存在于 team 4/0/1 条目、
   官方 testing map 与多张本体地图对象数据中 ⇒ **属本体可实例化单位集合**。
5. **保留意见**：尚无静态证据能**严格证明「沙盒编辑器 UI 列表中一定可直接点选」**，
   此项记为「强支持、尚未运行时验收」。

Sol 同时评价：本轮改走「消耗型建造单位」路径是**实质性收敛**，
不再是上一轮 `convertTo`/部署族里的强行解释。

## ⚠️ 未能确认项：官方中文名

`Strings_zh_cn.properties` 中**既无该移动单位、也无其生产建筑的中文本地化条目**。
故**无法从 1.15 中文语言文件确认官方中文名**。
本猜测为**按机制指认**，非按名称指认。若机制正确而名称表述不同，属表述误差。

## 若本猜仍错 —— 下一轮分叉点（已备好）

1. **「建造开始即死亡」 vs 「完成后被替换」**；
2. **「是否能选择多种建造结果」**（User 曾答「前三个方向都可以」，
   而该单位恰有 4 种可建造结果，覆盖资源 / 生产 / 功能 / 防御）。

## 环境（重要）

本机除 GitHub 外对公开网络存在 **DNS 拦截**（RW wiki、Fandom、namu.wiki 全部不可达）。
但**本机存在完整原版 1.15 游戏数据**，故机制判定**直接依据游戏数据本体**，
不再依赖二手 wiki。路径：`G:\deepseek 工作台\游戏环境\rustedwarfare PC 1.15 原版\assets\units\`

## Last writer

DeepSeek
