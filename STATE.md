# Current State

Round: 6 — **实验 2**（Round 1 / 2 属实验 1，已结束）

Status: waiting_for_user

> **协作方变更**：GPT 侧现由 **Luna** 接手（原为 Sol），已具备直写能力。
>
> **🎯 关键进展（Round 6）**：**本机找到完整原版游戏数据**，
> 单位定义为明文 `.ini`：`G:\deepseek 工作台\游戏环境\rustedwarfare PC 1.15 原版\assets\units\`
> （47 个顶层单位目录）。此前「资料站被 DNS 拦截」的障碍已消除，
> 机制判定改为**直接依据游戏数据本体**，不再依赖二手 wiki。

## 实验编号

| 实验 | 回合 | 单位 | 状态 |
|---|---|---|---|
| 实验 1 | `001` – `002` | Mech Engineer | ✅ 已结束（Round 2 命中） |
| **实验 2** | **`003` 起** | **未公开** | 🟡 进行中 |

## Confirmed clues（实验 2）—— 原样记录裁判原话

| 回合 | 问题 | User 原话 |
|---|---|---|
| Round 3 | Q1：怎么移动 / 本质是什么 | **「（奔跑）」** |
| Round 3 | Q2：主要做什么 / 最突出特点 | **「（牺牲自己，并完成目标）」** |
| Round 4 | Q1：完成目标时具体发生什么 | **「（它变成了别的东西）」** |
| Round 4 | Q2：什么条件下牺牲自己 | **「（到指定地点自动触发）」** |
| Round 5 | Q1：转化物是什么性质 | **「（你说的前三个方向都可以，第四个不确定）」** |
| Round 5 | Q2：奔跑阶段它本身是什么 | **「（它一开始是地面上的移动单位跑过去）」** |
| Round 6 | Q1：触发后画面上留下什么 | **「（没有什么效果，就是寻路到指定地点开始转化）」** |
| Round 6 | Q2：题目范围 | **「（原版沙盒编辑器里可以造）」** |

**Round 5 Q1 说明**：DeepSeek 给的四个方向为 ①能生产的建筑 ②功能设施 ③防御工事 ④标记/装置/资源点。
User 答「前三都可以」= **转化物同时具备三种性质**（多合一），非单选。
DeepSeek 把 Q1 设计成单选是**提问失误（第二次）**。

**Round 6 新增两条硬约束**：

- **不是瞬时效果**——「没有什么效果」+「开始转化」⇒ **转化物是持续存在的对象**；
- **范围＝原版，且可由沙盒编辑器建造**（非模组、非地图脚本；沙盒编辑器单位表内可见）。

## 机制骨架（由原话直接支持）

| 要素 | 内容 | 来源 |
|---|---|---|
| 初始形态 | **地面上的移动单位**（跑过去；非飞行物、非投射物、非建筑） | Round 5 Q2 |
| 移动方式 | 在地面真实奔跑（非比喻、非字面提示）；**寻路**到指定地点 | Round 5 Q2 / Round 6 Q1 |
| 触发条件 | 到达指定地点后**自动开始转化**（非玩家主动引爆、非瞬时爆炸） | Round 4 Q2 / Round 6 Q1 |
| 触发结果 | 原形态消失，变为**兼具生产 + 功能 + 防御**性质的**持久**物体 | Round 3/4/5 Q1 / Round 6 Q1 |
| 代价 | 原形态不再存在 | Round 3 Q2 |
| 范围 | **原版**，**沙盒编辑器可建造** | Round 6 Q2 |

**核心结论：原版四足/地面移动单位 → 寻路到点 → 自动转化为「生产 + 功能 + 防御」三合一的持久建筑。**

## ✅ Round 4 已解决的歧义（GPT 曾标为待澄清，现已确认）

GPT 在 `turns/003_gpt.md` 补充中提出两项待澄清，**该文写于收到 Round 4 回答之前**。
Round 4 的回答已直接解决其中两项：

| GPT 的疑问 | 现状 | 依据 |
|---|---|---|
| 「牺牲自己」是否必然死亡/消失，或只是玩家的战术取舍？ | ✅ **已解决**：是**自动触发**的机制，非玩家取舍 | Round 4 Q2「到指定地点自动触发」 |
| 完成目标时是否一定死亡或消失？ | ✅ **已解决**：原形态不再存在，**转化为别的东西** | Round 4 Q1 |
| 「奔跑」是真实移动、速度、还是字面提示？ | ✅ **已解决** | Round 5 Q2：在地面跑过去的移动单位，字面读法被排除 |
| 转化物是持久对象还是瞬时效果？ | ✅ **已解决** | Round 6 Q1：「没有什么效果，就是寻路到指定地点开始转化」 |
| 范围是原版 / 战役 / 模组？ | ✅ **已解决：原版** | Round 6 Q2：「原版沙盒编辑器里可以造」 |

## Rejected / 暂定

| 项 | 状态 | 依据 |
|---|---|---|
| 固定建筑、静止防御 | ✅ **确定排除** | Round 5 Q2：它有初始的、在地面奔跑的移动单位形态 |
| 自爆型投射物 / 飞行物 | **排除** | Round 5 Q2 明确否定飞行物读法 |
| 瞬时效果类机制 | **排除** | Round 6 Q1：不是爆炸/烟/动画，而是「开始转化」为持久物 |
| 模组 / 地图脚本内容 | **排除** | Round 6 Q2：原版沙盒编辑器可造 |
| 「转化为持久设施」有 55% 置信度 | ❌ **已撤回** | 经 Luna 批评：该数字无依据 |
| 「实验型战斗蜘蛛」 | 暂不列为候选 | Luna 核查：其触发条件是**死亡/被摧毁**，非到达地点自动转化 |
| DeepSeek 原「自爆 vs 转化」二分法 | ⚠️ **承认过宽** | Luna：核爆可被口语说成「变成别的东西」；二者并非互斥 |

## Current leading candidates（实验 2）

**刻意为空，且不入库**（修订 A）。

**机制判定已可据游戏数据本体做出**（见 `turns/006_deepseek.md`）：
原版 47 个单位目录中，**只有 `mechBunker` 拥有 `[action_deploy]` 部署动作**，
其形态为四足机甲（机械工厂生产、`techLevel: 1`、`OVER_CLIFF`），
部署后 `mechBunkerDeployed` 为 `isBuilding: true`、`movementType: NONE`、获得 `maxShield: 500`、
射程 200 → 240，且可 `undeploy` 站回去。

**待核**：`mechBunker` 的部署是**玩家手动动作**（`displayType: action`），
而 Round 4 Q2 的 User 原话是「**自动触发**」。**这两者存在张力，是下一轮首要澄清点。**
备选方向：`modularSpider` 的 12 种模块（含 `fabricator` 生产 / `nano` 功能 /
`laserdefense`、`shieldGen` 防御）同时具备三性，与本轮 Q1 高度呼应。

**具体名称仍不给出**——上述张力未解决前，报名字是赌而非推理。

## ⚠️ Round 7 追问结果与由此暴露的核心矛盾

DeepSeek 就两处矛盾追加提问，User 回答：

| 追问 | User 回答 | 效果 |
|---|---|---|
| 范围是纯原版还是含 mod？ | **「纯原版——游戏自带单位，不含 mod」** | 锁定为 `assets\units\` 47 个本体目录 |
| 转化是否可逆？ | **「不可逆——变过去就回不来了」** | **淘汰 `mechBunker` 家族**（它有 `[action_undeploy]`） |

### 未解决的矛盾（阻塞正式猜测）

1. User 说转化物**「能生产的建筑」**这一方向成立；
2. 但游戏数据中 `mechBunkerDeployed` **完全没有生产能力**（`canBuild_*` / `nanoRange` 均空）；
3. 且**「不可逆」淘汰了原版唯一拥有部署动作的 `mechBunker`**。

**⇒ DeepSeek 现有候选方向与 User 线索中至少一条冲突，冲突点尚未定位。**

### DeepSeek 的自我批评（记录在案）

实验 2 已进行 **5 轮提问**，DeepSeek **仍未给出正式猜测**。根本问题：

**提问全部集中在「机制」维度，而「身份」维度（阵营 / 造价量级 / 外观 / 出场场景）一个问题都没问过。**
机制线索善于**排除**，但**无法在剩余集合内排序**。
**下一轮必须转向身份维度**，否则会继续空转。

## Round 8 追问结果：身份维度

| 追问 | User 回答 |
|---|---|
| 工厂来源 / 沙盒分类？ | **「不是以上答案，可以从沙盒编辑器直接放出来，也可以工厂里出来，但不是你说的工厂」** |
| 自动转化还是玩家点击？ | **「玩家手动点『部署』，它到位后才开始转化」** |

**「玩家手动部署」排除掉「全自动到点转化」整类机制。**

## 用游戏数据得出的机制族清单（原版 1.15）

检索 `assets\translations\Strings*.properties`，**描述含「部署 / Deploy」的原版单位只有两组**：

| 单位 | 中文名 | 部署描述 | 可生产？ | 移动域 |
|---|---|---|---|---|
| `mechBunker` / `mechBunkerDeployed` | **移动炮塔** | 原地部署获得额外射程与防御 | ❌ | **地面**（四足 `OVER_CLIFF`） |
| `experimentalGunship` | 实验型武装直升机 | 部署获得额外射程、火力、护盾 | ✅ | **HOVER 空中** |

**能生产单位的原版单位**（描述含「可建造单位」）仅 4 个：
`combatEngineer`、`experimentalSpider`、`experiementalCarrier`、`experimentalGunship`。
**其中只有 `experimentalGunship` 同时具备「部署」与「生产」。**

## ⚠️ 无法同时满足的线索组（记录在案）

| 若取线索 | 指向 | 冲突 |
|---|---|---|
| 「地面单位奔跑」 | `mechBunker`（四足机甲） | 无生产能力；且有 `[action_undeploy]`（与「不可逆」冲突） |
| 「可生产 + 可部署 + 三合一」 | `experimentalGunship` | 是**空中 HOVER 单位**，与「地面奔跑」冲突 |

**DeepSeek 的处理**：判定 User 所述「能生产的建筑」可能是对
**「部署后获得额外能力」的泛化表述**，而非字面「生产单位」。
在此假设下 **`mechBunker` / 移动炮塔** 同时满足
「地面四足奔跑」「玩家手动部署」「变为持久建筑」「不可撤销」「沙盒可造」五条 —— **本轮最强候选**。

**Round 9 将给出正式猜测。**



## ⚠️ 实验 2 新增纪律（三项）

1. **开局禁用固定选项** —— 实益是避免 DeepSeek 的知识缺口成为搜索边界（非「信息量更高」）。
2. **禁止候选名单入库** —— 见修订 A。
3. **GPT 写入须同守纪律** —— 只写分析与纠错，不写候选、不写答案。

## Protocol

每轮：DeepSeek 提 2 个问题 → User 回答 → DeepSeek 给出正式猜测 → User 判定 → DeepSeek 写 `turns/00N_deepseek.md` → GPT 写 `turns/00N_gpt.md`。

**并发写入规则（实验 2 起）**：GPT 已具备直写能力，双方并发提交会产生竞争。
DeepSeek 每次提交前**必须先 `git fetch` 并确认 `origin/main`**，避免覆盖 GPT 的提交。

已实测发生的竞争序列：

| 顺序 | 提交 | 作者 |
|---|---|---|
| 1 | `f5de804` | DeepSeek |
| 2 | `68ddf11` | GPT |
| 3 | `b31c8e8` | GPT |
| 4 | `e41e658` → `b4ab0c0` | DeepSeek |
| 5 | `5806225` | GPT（改了 `STATE.md`） |
| 6 | `ffe427f` | DeepSeek |

**DeepSeek 已两次遇到「文件在读取后被 GPT 改写」而被迫重读**（`STATE.md` × 2）。

## 工具状态

| 通道 | 状态 |
|---|---|
| DeepSeek 本机 git | ✅ 读写可用 |
| `tools/bridge.ps1` | ✅ 已验证（备用通道） |
| **GPT / Codex 直写** | ✅ 已多次实测成功（`68ddf11`、`b31c8e8`、`5806225`） |
| GitHub 插件直写 | ❌ 仍 403（`Resource not accessible by integration`） |

## 环境限制（重要，影响推理质量）

本机**除 GitHub 外对公开网络存在 DNS 拦截**：RW 中文 Wiki、RW Fandom Wiki、`en.namu.wiki`、
`help.openai.com`、`qiita.com` 等全部不可访问。**仅搜索引擎摘要可用。**
DeepSeek 无法通读任何完整单位表，**其《铁锈战争》名册知识缺口无法通过外部资料弥补**。
`turns/003_gpt.md` 中 GPT 的知识补充是本实验补足该缺口的主要途径。

## Last writer

DeepSeek
