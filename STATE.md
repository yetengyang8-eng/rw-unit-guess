# Current State

Round: 5 — **实验 2**（Round 1 / 2 属实验 1，已结束）

Status: waiting_for_user

## 实验编号

| 实验 | 回合 | 单位 | 状态 |
|---|---|---|---|
| 实验 1 | `001` – `002` | Mech Engineer | ✅ 已结束（Round 2 命中） |
| **实验 2** | **`003` 起** | **未公开** | 🟡 进行中 |

## Confirmed clues（实验 2）—— 原样记录裁判原话

| 回合 | 问题 | User 原话 |
|---|---|---|
| Round 3 | Q1：怎么移动 / 本质是什么 | **「（奔跑）」** |
| Round 3 | Q2：主要做什么 / 最突出特点 | **「（牺牲自己，并完成目标）」** |
| Round 4 | Q1：完成目标时具体发生什么 | **「（它变成了别的东西）」** |
| Round 4 | Q2：什么条件下牺牲自己 | **「（到指定地点自动触发）」** |

## 机制骨架（由原话直接支持）

| 要素 | 内容 | 来源 |
|---|---|---|
| 移动 | 「奔跑」 | Round 3 Q1 |
| 触发条件 | **到达指定地点后自动触发** | Round 4 Q2 |
| 触发结果 | **转化为另一种存在**（非单纯爆炸消失） | Round 4 Q1 |
| 代价 | 原形态不再存在 | Round 3 Q2 |

**核心结论：这是一个「移动到指定地点 → 自动转化为另一存在」的单位。**

## ✅ Round 4 已解决的歧义（GPT 曾标为待澄清，现已确认）

GPT 在 `turns/003_gpt.md` 补充中提出两项待澄清，**该文写于收到 Round 4 回答之前**。
Round 4 的回答已直接解决其中两项：

| GPT 的疑问 | 现状 | 依据 |
|---|---|---|
| 「牺牲自己」是否必然死亡/消失，或只是玩家的战术取舍？ | ✅ **已解决**：是**自动触发**的机制，非玩家取舍 | Round 4 Q2「到指定地点自动触发」 |
| 完成目标时是否一定死亡或消失？ | ✅ **已解决**：原形态不再存在，**转化为别的东西** | Round 4 Q1 |
| 「奔跑」是真实移动、速度、还是字面提示？ | ⏳ **仍未解决**（Round 5 处理） | — |

## Rejected / 暂定

| 项 | 状态 | 依据 |
|---|---|---|
| 固定建筑、静止防御 | ⚠️ **暂定排除**（非已排除） | GPT 指出：「奔跑」若为字面/比喻提示，此项排除不成立 |
| 自爆型攻击单位 | 排除 | Round 4 Q1「变成别的东西」与「爆炸后消失」不符 |
| 「实验型战斗蜘蛛」 | 暂不列为候选 | 搜索摘要称其机制为「死后原地核爆」＝消失并造成伤害，**非转化**。此排除**存疑**，已请 GPT 复核 |

## Current leading candidates（实验 2）

**刻意为空，且不入库**（修订 A）。

**方向性判断（非候选）**：机制属「**转化型**」分支，即移动体落地后成为持久存在物。
置信度中等（约 55%）。**具体名称不给出**——Round 4 外部资料核查失败，无法在分支内可靠枚举。

## ⚠️ 实验 2 新增纪律（三项）

1. **开局禁用固定选项** —— 实益是避免 DeepSeek 的知识缺口成为搜索边界（非「信息量更高」）。
2. **禁止候选名单入库** —— 见修订 A。
3. **GPT 写入须同守纪律** —— 只写分析与纠错，不写候选、不写答案。

## Protocol

每轮：DeepSeek 提 2 个问题 → User 回答 → DeepSeek 给出正式猜测 → User 判定 → DeepSeek 写 `turns/00N_deepseek.md` → GPT 写 `turns/00N_gpt.md`。

**并发写入规则（实验 2 起）**：GPT 已具备直写能力，双方并发提交会产生竞争。
DeepSeek 每次提交前**必须先 `git fetch` 并确认 `origin/main`**，避免覆盖 GPT 的提交。

已实测发生的竞争序列：

| 顺序 | 提交 | 作者 |
|---|---|---|
| 1 | `f5de804` | DeepSeek |
| 2 | `68ddf11` | GPT |
| 3 | `b31c8e8` | GPT |
| 4 | `e41e658` → `b4ab0c0` | DeepSeek |
| 5 | `5806225` | GPT（改了 `STATE.md`） |
| 6 | `ffe427f` | DeepSeek |

**DeepSeek 已两次遇到「文件在读取后被 GPT 改写」而被迫重读**（`STATE.md` × 2）。

## 工具状态

| 通道 | 状态 |
|---|---|
| DeepSeek 本机 git | ✅ 读写可用 |
| `tools/bridge.ps1` | ✅ 已验证（备用通道） |
| **GPT / Codex 直写** | ✅ 已多次实测成功（`68ddf11`、`b31c8e8`、`5806225`） |
| GitHub 插件直写 | ❌ 仍 403（`Resource not accessible by integration`） |

## 环境限制（重要，影响推理质量）

本机**除 GitHub 外对公开网络存在 DNS 拦截**：RW 中文 Wiki、RW Fandom Wiki、`en.namu.wiki`、
`help.openai.com`、`qiita.com` 等全部不可访问。**仅搜索引擎摘要可用。**
DeepSeek 无法通读任何完整单位表，**其《铁锈战争》名册知识缺口无法通过外部资料弥补**。
`turns/003_gpt.md` 中 GPT 的知识补充是本实验补足该缺口的主要途径。

## Last writer

DeepSeek
