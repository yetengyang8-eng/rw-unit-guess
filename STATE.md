# Current State

Round: 10 — **实验 2 已结束（✅ DeepSeek 猜测正确）**

Status: experiment_2_complete

> **📖 想快速了解实验 2 发生了什么？只读 `turns/EXPERIMENT2_SUMMARY.md` 即可（全程整理报告）。**

## 实验总览

| 实验 | 回合 | 单位 | 结果 | 轮数 |
|---|---|---|---|---|
| 实验 1 | `001` – `002` | Mech Engineer | ✅ 命中 | 2 轮 |
| **实验 2** | **`003` – `010`** | **`bugSpore`** | ✅ **命中** | **10 轮** |

## 实验 2 最终结果

**裁判私下选定：`bugSpore`（虫族孢子 / 消耗型建造单位）**
**DeepSeek 第 3 次正式猜测命中。**

完整论证见 `turns/009_deepseek.md`；全程整理见 `turns/EXPERIMENT2_SUMMARY.md`。

## 实验 2 的 Confirmed clues（原样记录裁判原话）

| 回合 | 问题 | User 原话 |
|---|---|---|
| 3 | 怎么移动 / 本质是什么 | **「（奔跑）」** |
| 3 | 主要做什么 / 最突出特点 | **「（牺牲自己，并完成目标）」** |
| 4 | 完成目标时具体发生什么 | **「（它变成了别的东西）」** |
| 4 | 什么条件下牺牲自己 | **「（到指定地点自动触发）」** |
| 5 | 转化物是什么性质 | **「（你说的前三个方向都可以，第四个不确定）」** |
| 5 | 奔跑阶段它本身是什么 | **「（它一开始是地面上的移动单位跑过去）」** |
| 6 | 触发后留下什么 | **「（没有什么效果，就是寻路到指定地点开始转化）」** |
| 6 | 题目范围 | **「（原版沙盒编辑器里可以造）」** |
| 7 | 工厂来源 | **「不是以上答案，可以从沙盒编辑器直接放出来，也可以工厂里出来，但不是你说的工厂」** |
| 7 | 自动还是手动 | **「玩家手动点『部署』，它到位后才开始转化」** |
| 8 | 纯原版还是含 mod | **「纯原版——游戏自带单位，不含 mod」** |
| 8 | 转化是否可逆 | **「不可逆——变过去就回不来了（不可撤销）」** |
| 9 | 判定猜测 1 | **「不是实验悬浮型气垫船」** + 三条新约束 |
| 10 | 判定猜测 3 | **「你的答案正确，就是 bugSpore」** |

## 命中的机制（本机游戏数据核实）

`assets\units\classic_bugs\bug_spore\bug_spore.ini`：
`movementType: LAND` + **`dieOnConstruct: true`** + `useAsBuilder: true`
+ `canBuild_1..4`（`bugExtractor` / **`bugNest`** / `bugGenerator` / `bugTurret`）
+ `builtFrom_1_name: bugNest`

**排他性**：全原版只有 `bugSpore`（LAND）与 `bugFly`（AIR）带 `dieOnConstruct`。

## ⚠️ 待 User 决定的规则修订 D

**审计发现**：`bugSpore` 于 commit `6b74101`（23:26:11）**已被写入公开仓库**，
而裁判判定在**其后**才公布。即**猜测名称在判定前已入库**。

这与实验 1 属同一类问题（实验 1 中「Mech Engineer」以候选身份提前入库），但实验 2 更直接。
需要说明的是：**本次泄露不影响 DeepSeek 的推理有效性**——猜测由 DeepSeek 自身推理得出，
仓库只是记录载体，不存在「从仓库读到答案再假装猜中」。

**根因**：`README.md` 规则 1 要求「真正答案不得提前写入 GitHub」，
但**未规定「猜测本身是否算答案」**；而协议又要求「DeepSeek 写 `turns/00N_deepseek.md`」。
两条要求在「猜测已接近答案」时冲突。

**建议修订 D（待决定）**：判定前的正式猜测，其具体名称不得入库；
入库只写「已提交一猜，待判定」，判定后再于结果文件中记录。

## 规则修订现状（`README.md` 的 `## 实验修订` 一节）

| 修订 | 内容 | 状态 |
|---|---|---|
| A | 候选名单不入库 | ✅ 生效 |
| B | 开局禁用固定选项（防知识缺口变成搜索边界） | ✅ 生效 |
| C | GPT 写入须同守纪律 | ✅ 生效 |
| **D** | **判定前猜测名称不入库** | ⏳ **待 User 决定** |

## 环境与工具

| 项 | 状态 |
|---|---|
| 本机原版游戏数据 | ✅ `G:\deepseek 工作台\游戏环境\rustedwarfare PC 1.15 原版\assets\units\`（47 个顶层目录，明文 `.ini`） |
| 本机公开网络 | ⚠️ **除 GitHub 外存在 DNS 拦截**（RW wiki / Fandom / namu.wiki 均不可达） |
| DeepSeek 本机 git | ✅ 读写可用 |
| **GPT-5.6 Sol 直写** | ✅ 可用（见 `GPT_SOL_ONLINE.md`） |
| `tools/bridge.ps1` | ✅ 备用通道，已验证 |
| GitHub 插件直写 | ❌ 仍 403（`Resource not accessible by integration`） |
| **并发写入竞争** | ⚠️ 双方并发提交多次冲突，已采用「提交前 fetch + push 失败则 rebase 重试」 |

## Last writer

DeepSeek
