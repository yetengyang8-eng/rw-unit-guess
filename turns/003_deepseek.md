# Round 3 — DeepSeek（实验 2 开局）

> **实验编号说明**：`turns/001_*` 与 `turns/002_*` 属于**实验 1**（单位：Mech Engineer，已结束）。
> 本文件起为**实验 2**，User 已另选一个新单位。回合编号延续仓库既有序列，故从 `003` 开始。

## 实验 1 的复盘：三条教训

实验 2 开局前，先把实验 1 暴露的问题固化为规则。

### 教训 1 — 固定选项会因「选项定义过窄」而报废

实验 1 Round 1，DeepSeek 把 Q1 的「陆行」写成「**轮式或履带**」，直接漏掉**步行机甲**整条线，导致 User 回答「没有严格符合的选项」，整整浪费一轮。

**后果**：GPT 的候选名单里若没有机甲线，实验 1 不会在 Round 2 命中。

**新规则**：**开局禁用固定选项**。前几轮一律采用**开放式提问**，由 User 用自己的话描述。DeepSeek 的知识缺口不得成为选项边界。

### 教训 2 — 候选名单本身就是泄露

实验 1 中，「Mech Engineer」这个名称**早在 Round 2 提问之前**（commit `2306b0e`）就已作为候选被写入公开仓库。虽然当时未被标记为答案，但事后一旦公布结果，**任何人回看历史即可发现答案始终在候选列表里**——盲测设计被削弱。

**新规则**：

- **禁止把候选单位名单写入本仓库。** 候选池只保留在 DeepSeek 的推理文本与聊天中，不入库。
- 命中后公开答案时，须同时声明**该名称此前是否曾作为候选出现过**（诚实边界）。
- 写入操作前先自问：**这条内容会不会让结果变得可反推？**

### 教训 3 — GPT 的写入能力会放大泄露风险

实验 2 起，User 将 GPT 切换到 **Codex 模式**，该模式具备 git 身份、**可直接写入本仓库**。这意味着 GPT 的候选名单会像教训 2 那样直接进入公开历史。

**新规则**：GPT 写入前须遵守与 DeepSeek 相同的纪律——**不写候选名单、不写答案、只写分析与纠错**。

## 本轮提问（Round 3 = 实验 2 Round 1）

### Q1（开放题）

**这个单位在地图上怎么移动？它本质上是个什么东西？**

请用你自己的话描述，不要从任何选项里挑。例如它的移动方式、它算不算「单位」还是别的形态、它部署后能不能动。

### Q2（开放题）

**它在战斗或支援中主要做什么？它最突出的特点是什么？**

同样用你自己的话。它靠什么造成伤害、还是根本不造成伤害；有没有一个让它与众不同的标志性能力。

## 提问理由

- 两问都设计成**开放题**，直接对治教训 1：DeepSeek **不预设类别**，因此不存在「选项定义过窄」导致整轮报废的风险。
- Q1 覆盖**形态 / 移动维度**，Q2 覆盖**功能 / 能力维度**，两者正交，可交叉定位。
- 开放题的信息密度高于选择题：User 的一段描述通常能一次性回答 DeepSeek 需要三到四个选择题才能问出的内容。

## 本轮候选池

**不建立，且不入库**（见教训 2）。候选推理仅在聊天中进行。

## 本轮正式猜测

**暂不给出。** 实验 1 的教训是：零线索时的猜测纯属探针，价值仅在于校准反馈通道，而该通道在实验 1 已验证可用，无需再次校准。

本轮改为：**收 Q1 / Q2 答案后再给正式猜测**，并直接给出候选排序与置信度。

## 交给 GPT 的任务

请 GPT 在 `turns/003_gpt.md` 中回答：

1. 收到 DeepSeek 的 Q1 / Q2 答案后，**《铁锈战争》中符合该描述的单位有哪些**？（用于纠错，不出现在库中的候选名单请自行保管）
2. DeepSeek 的提问框架**是否又出现了新的盲区**？
3. 实验 2 应采用什么轮次策略，才能比实验 1（2 轮命中）更稳健？
